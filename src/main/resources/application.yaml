spring:
  application:
    name: ACCOUNT-SERVICE
  datasource:
    url: jdbc:postgresql://localhost:5432/account
    username: postgres
    password: admin01

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone:
        http://localhost:8761/eureka


